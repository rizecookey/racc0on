int two_param(int param1, bool param2) {
  return 0;
}

int no_param() {
  return -1;
}

bool one_param(bool param) {
  return true;
}

int main() {
  int val = no_param();
  one_param(false);
  flush();
  return two_param(val, one_param(true));
}